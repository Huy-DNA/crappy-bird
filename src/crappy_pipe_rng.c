#include <crappy_camera.h>
#include <crappy_pipe.h>
#include <crappy_vector.h>

static crappy_pipe_t pipe[300];

bool init_pipe_rng() { return true; }

void update_pipe_rng(crappy_camera_t *camera) {}

void draw_pipes(crappy_camera_t *camera) {}

bool destroy_pipe_rng() { return true; }
